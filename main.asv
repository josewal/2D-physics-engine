clc, clear
dt = 0.1;
w = World()

for i = 1:50
    b.applyForce([0,-1])
    b.update(dt)
    
    w.body.plotBody()
    hold on
    body.plotBonds()
    hold on
    body.plotCOM();
    hold off
    
    pause(dt)
end